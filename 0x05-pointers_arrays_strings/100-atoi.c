#include <string.h>

/**
 * _atoi - converts a string to an integer
 * @s: string to convert
 */

int _atoi(char *s)
{
	int length, i, result;

	length = strlen(s);

	for (i = 0; i < length; i++)
	{

	}
}
