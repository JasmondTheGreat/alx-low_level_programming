#include <stdio.h>

/**
 * main - Entry point of the program
 * Prints the name of the file using the __FILE__ predefined macro
 * Return: 0
 */

int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
