#include <stdio.h>

void _putchar(char *str)
{
	putchar(str);
}

/**
 * print_number - prints an integer
 * @n: integer to be printed
 */

void print_number(int n)
{
	int len, i, numCopy, mult;

	mult = 1;
	len = 0;

	while (numCopy != 0)
	{
		numCopy = numCopy / 10;
		len++;
	}

	for (i = 0; i < len; i++)
	{
		``
	}


}
